{
  "features": [
    {
      "id": "tts-settings-backend",
      "description": "Extend AppSettings interface to include ttsVoice, ttsRate, ttsPitch. Add TTS settings to GET /api/settings response. Add TTS settings to PUT /api/settings endpoint. Validate TTS settings (voice from curated list, rate -50% to +100%, pitch -50Hz to +50Hz).",
      "skillName": "backend-worker",
      "milestone": "tts-settings",
      "preconditions": [
        "TTS service exists at backend/services/tts.ts with synthesize() accepting voice/rate/pitch options",
        "Settings service exists at backend/services/settings.ts with getSettings() and updateSettings()",
        "Settings API routes exist at backend/routes/settings.ts with GET and PUT endpoints"
      ],
      "expectedBehavior": [
        "AppSettings interface includes ttsVoice (string), ttsRate (string), ttsPitch (string)",
        "GET /api/settings returns { ttsVoice, ttsRate, ttsPitch } in response",
        "PUT /api/settings accepts { ttsVoice, ttsRate, ttsPitch } and persists them",
        "Validation: voice must be one of 6 curated voices, rate must be -50% to +100%, pitch must be -50Hz to +50Hz",
        "Invalid TTS settings return 400 with descriptive error"
      ],
      "verificationSteps": [
        "npm run test -- --grep 'tts' (expect tests for TTS settings)",
        "curl GET /api/settings shows ttsVoice, ttsRate, ttsPitch",
        "curl PUT /api/settings with valid TTS settings returns 200",
        "curl PUT /api/settings with invalid voice returns 400"
      ],
      "fulfills": ["VAL-TTS-005"],
      "status": "pending"
    },
    {
      "id": "tts-settings-frontend",
      "description": "Add TTS settings section to Settings page. Voice dropdown with 6 curated voices (en-US-GuyNeural, en-US-ChristopherNeural, en-US-BrianNeural, en-GB-ThomasNeural, en-US-JennyNeural, en-US-MichelleNeural). Rate slider (-50% to +100%, default +25%). Pitch slider (-50Hz to +50Hz, default +0Hz). Save changes via PUT /api/settings. Show current settings on page load.",
      "skillName": "frontend-worker",
      "milestone": "tts-settings",
      "preconditions": [
        "Settings page exists at src/pages/Settings.tsx",
        "settingsApi.ts exists with updateSettings() method",
        "SettingsResponse type exists in settingsApi.ts"
      ],
      "expectedBehavior": [
        "New 'Voice Output' section appears in Settings page below AI Model Selection",
        "Voice dropdown shows 6 curated voices with en-US-GuyNeural selected by default",
        "Rate slider shows -50% to +100% range with current value displayed",
        "Pitch slider shows -50Hz to +50Hz range with current value displayed",
        "Changing any setting triggers PUT /api/settings and shows success toast",
        "Settings persist after page refresh (GET /api/settings on mount)"
      ],
      "verificationSteps": [
        "npm run typecheck (expect no errors)",
        "Manual: Navigate to Settings, verify TTS section exists with all 3 controls",
        "Manual: Change voice, verify toast shows success",
        "Manual: Refresh page, verify settings are preserved"
      ],
      "fulfills": ["VAL-TTS-001", "VAL-TTS-002", "VAL-TTS-003", "VAL-TTS-004"],
      "status": "pending"
    },
    {
      "id": "tts-voice-integration",
      "description": "Connect TTS settings to VoiceControls. Modify voiceAPI.synthesize() to use saved TTS voice/rate/pitch from settings instead of defaults. Fetch TTS settings on app load and cache them. Pass voice/rate/pitch to synthesize() call in VoiceControls.",
      "skillName": "frontend-worker",
      "milestone": "tts-settings",
      "preconditions": [
        "VoiceControls.tsx calls voiceAPI.synthesize({ text }) without voice/rate/pitch",
        "voiceApi.ts synthesize() accepts voice/rate/pitch options",
        "Settings API returns TTS settings"
      ],
      "expectedBehavior": [
        "On app load, fetch TTS settings from GET /api/settings",
        "Cache TTS settings (voice, rate, pitch) in a module-level variable or Zustand store",
        "VoiceControls.speak() calls voiceAPI.synthesize() with cached voice/rate/pitch",
        "Meditation session voice playback uses the saved TTS voice settings"
      ],
      "verificationSteps": [
        "npm run typecheck (expect no errors)",
        "Manual: Set voice to en-US-ChristopherNeural in Settings",
        "Manual: Start meditation, enable voice output, verify different voice is used"
      ],
      "fulfills": [],
      "status": "completed"
    },
    {
      "id": "fix-auth-onboarding-blocker",
      "description": "Fix pre-existing auth middleware issue that blocks profile creation. Auth middleware (commit eb9dfb0) broke POST /api/profile - it returns 401 because /api/profile is not in public paths. Fix: Add POST /api/profile to public paths in auth middleware. AFTER THIS FIX: Re-validate VAL-TTS-006 and VAL-CROSS-001.",
      "skillName": "backend-worker",
      "milestone": "fix-auth-blocker",
      "preconditions": [
        "Auth middleware exists in backend"
      ],
      "expectedBehavior": [
        "POST /api/profile works without auth token (public path)",
        "Profile creation succeeds from frontend onboarding flow",
        "User can access meditation session after profile creation"
      ],
      "verificationSteps": [
        "Manual: Create profile via frontend, verify success",
        "Manual: Access meditation session after profile creation"
      ],
      "fulfills": ["VAL-TTS-006", "VAL-CROSS-001"],
      "status": "pending"
    },
    {
      "id": "stt-settings-backend",
      "description": "Add STT model settings to backend. Add sttModel field to AppSettings. Add GET /api/settings/stt-models endpoint that lists available sherpa-onnx models from servers/stt/ directory with their sizes. Add POST /api/settings/stt-reload endpoint that hot-reloads the STT model (destroys old recognizer, creates new one with specified model). NOTE: This feature sets up the API endpoints and validation. True hot-reload implementation is done by stt-hot-reload-impl feature.",
      "skillName": "backend-worker",
      "milestone": "stt-settings",
      "preconditions": [
        "STT service exists at backend/services/stt.ts with SttService class",
        "Sherpa-onnx server at servers/stt/server.mjs loads model at startup",
        "Settings service exists at backend/services/settings.ts"
      ],
      "expectedBehavior": [
        "GET /api/settings/stt-models returns [{ name, path, sizeMB, memoryMB }] for each model in servers/stt/",
        "POST /api/settings/stt-reload accepts { modelDir } and triggers model hot-reload",
        "Hot-reload stops current recognizer, creates new one with specified model",
        "Returns 200 on success, 400 if model not found, 500 if reload fails",
        "STT server /health endpoint continues to return 200 during/after reload"
      ],
      "verificationSteps": [
        "npm run test -- --grep 'stt' (expect tests for STT model endpoints)",
        "curl GET /api/settings/stt-models returns list of available models",
        "curl POST /api/settings/stt-reload with valid model returns 200",
        "curl http://localhost:8765/health returns 200 after reload"
      ],
      "fulfills": ["VAL-STT-004", "VAL-STT-006"],
      "status": "pending"
    },
    {
      "id": "stt-hot-reload-impl",
      "description": "Implement true hot-reload for STT models by modifying servers/stt/server.mjs to support a POST /reload endpoint that dynamically switches Whisper models without restarting the server. The endpoint should: (1) Stop the current recognizer, (2) Detect the new model config from the specified model directory, (3) Create a new recognizer with the new model, (4) Return success. The backend routes/stt.ts should proxy /api/stt/reload to the STT server's /reload endpoint.",
      "skillName": "backend-worker",
      "milestone": "stt-settings",
      "preconditions": [
        "STT server at servers/stt/server.mjs exists and loads model at startup",
        "Backend routes/stt.ts exists and proxies to STT server"
      ],
      "expectedBehavior": [
        "STT server has POST /reload endpoint that accepts { modelDir } parameter",
        "Reload stops old recognizer, creates new one with specified model",
        "During reload, /health returns { status: 'reloading', model_loaded: false }",
        "After reload, /health returns { status: 'ok', model_loaded: true }",
        "Backend /api/stt/reload proxies to STT server /reload endpoint",
        "Concurrent transcription requests during reload return 503"
      ],
      "verificationSteps": [
        "curl -X POST http://localhost:8765/reload -d '{\"modelDir\": \"sherpa-onnx-whisper-tiny.en\"}'",
        "Verify old transcription requests fail during reload",
        "Verify new transcription requests succeed after reload"
      ],
      "fulfills": [],
      "status": "pending"
    },
    {
      "id": "stt-settings-frontend",
      "description": "Add STT settings section to Settings page. Model dropdown listing available sherpa-onnx models with size info. Warning message when switching to larger model. 'Reload Model' button triggers POST /api/settings/stt-reload. Loading indicator during reload. Error toast if reload fails.",
      "skillName": "frontend-worker",
      "milestone": "stt-settings",
      "preconditions": [
        "Settings page exists at src/pages/Settings.tsx",
        "Settings API client exists with getSettings() method",
        "STT models endpoint exists at GET /api/settings/stt-models"
      ],
      "expectedBehavior": [
        "New 'Speech Recognition (STT)' section appears in Settings page",
        "Model dropdown shows available models with size (e.g., 'whisper-tiny.en (~40MB)')",
        "Warning banner appears when selected model is larger than current",
        "Reload Model button triggers POST /api/settings/stt-reload",
        "Loading spinner shows while model is reloading (2-5 seconds)",
        "Success toast on completion, error toast on failure",
        "Previous model remains active if reload fails"
      ],
      "verificationSteps": [
        "npm run typecheck (expect no errors)",
        "Manual: Navigate to Settings, verify STT section exists",
        "Manual: Select different model, verify warning appears for larger models",
        "Manual: Click Reload, verify loading indicator, verify success"
      ],
      "fulfills": ["VAL-STT-001", "VAL-STT-002", "VAL-STT-003", "VAL-STT-005", "VAL-STT-008"],
      "status": "pending"
    },
    {
      "id": "stt-model-integration",
      "description": "Ensure voice input uses the currently loaded STT model. After STT model reload completes, voice input in VoiceControls should use the new model for transcription. The voiceInputManager calls voiceAPI.transcribe() which proxies to the STT service.",
      "skillName": "frontend-worker",
      "milestone": "stt-settings",
      "preconditions": [
        "VoiceControls.tsx calls voiceAPI.transcribe() for voice input",
        "voiceApi.ts transcribe() proxies to /api/stt/transcribe",
        "STT service proxies to sherpa-onnx server on port 8765"
      ],
      "expectedBehavior": [
        "Voice input in meditation session uses the active STT model",
        "After model reload, new transcriptions use the reloaded model",
        "If reload is in progress, transcription requests wait or return appropriate error"
      ],
      "verificationSteps": [
        "Manual: Reload STT model in Settings to a different one",
        "Manual: Start meditation, use voice input, verify transcription works with new model"
      ],
      "fulfills": ["VAL-STT-007", "VAL-CROSS-002", "VAL-CROSS-003"],
      "status": "pending"
    }
  ]
}
